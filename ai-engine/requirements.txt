# Python dependencies for ScrowPay AI Risk Engine
# Synthetic data generation and model training

# Core data science libraries
numpy>=1.26.0
pandas>=2.2.0

# Machine learning
scikit-learn>=1.3.0

# Model persistence
joblib>=1.3.0

# Web framework (for API)
flask>=3.0.0
# Cross-origin support so the frontend (served from :8080) can call this
# service running on :5000 without browser-level blocking.
flask-cors>=4.0.0

# Resend SDK — transactional email proxy. The Python service holds the
# API key so we never expose it to the browser. Endpoints exposed:
#   POST /api/v1/notify/email   (generic transactional send)
#   POST /api/v1/notify/otp     (signup / sensitive-action OTP)
resend>=2.0.0

# .env loader so RESEND_API_KEY etc. can live in ai-engine/.env without
# being shell-exported manually each run.
python-dotenv>=1.0.0

# Optional: For better performance
# numba==0.58.1
